python3 << 'PYEOF'
# Load all base64 images
imgs = {}
names = ['reticula', 'colored_perspective', 'floor_plan', 'wood_model',
         'aerial', 'figureground', 'network', 'program', 'a6_final',
         'toaster1', 'toaster2']
for n in names:
    with open(f'/tmp/{n}.b64') as f:
        imgs[n] = f.read().strip()

html = f'''<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Jesus Moya-Gutierrez — Architecture Portfolio</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
<style>
  :root {{
    --ink: #0f0e09;
    --paper: #f4efe6;
    --red: #b5291e;
    --yellow: #e5d03e;
    --mid: #7a6f5e;
    --rule: 1px solid #c5bcaa;
    --dark-bg: #111009;
  }}

  *, *::before, *::after {{ box-sizing: border-box; margin: 0; padding: 0; }}
  html {{ scroll-behavior: smooth; }}

  body {{
    background: var(--paper);
    color: var(--ink);
    font-family: 'Cormorant Garamond', Georgia, serif;
    overflow-x: hidden;
    cursor: default;
  }}

  /* ══════════════════════ NAV ══════════════════════ */
  nav {{
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 900;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 4vw;
    background: rgba(244,239,230,0.92);
    backdrop-filter: blur(8px);
    border-bottom: var(--rule);
  }}

  .nav-name {{
    font-family: 'Space Mono', monospace;
    font-size: 0.58rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--mid);
  }}

  .nav-links {{
    display: flex;
    gap: 2rem;
    list-style: none;
  }}

  .nav-links a {{
    font-family: 'Space Mono', monospace;
    font-size: 0.55rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    text-decoration: none;
    color: var(--mid);
    transition: color 0.2s;
  }}

  .nav-links a:hover {{ color: var(--red); }}

  /* ══════════════════════ COVER ══════════════════════ */
  .cover {{
    min-height: 100vh;
    padding-top: 60px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    position: relative;
    overflow: hidden;
  }}

  .cover-left {{
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 8vw;
    gap: 2rem;
    position: relative;
    z-index: 1;
  }}

  .cover-eyebrow {{
    font-family: 'Space Mono', monospace;
    font-size: 0.6rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--mid);
  }}

  .cover-name {{
    font-size: clamp(3rem, 6vw, 6rem);
    font-weight: 300;
    line-height: 0.92;
    letter-spacing: -0.02em;
  }}

  .cover-name em {{
    font-style: italic;
    color: var(--red);
  }}

  .cover-details {{
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    border-top: var(--rule);
    padding-top: 1.2rem;
  }}

  .cover-details span {{
    font-family: 'Space Mono', monospace;
    font-size: 0.6rem;
    letter-spacing: 0.15em;
    color: var(--mid);
  }}

  .cover-badge {{
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    border: 1.5px solid var(--red);
    color: var(--red);
    font-family: 'Space Mono', monospace;
    font-size: 0.55rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    padding: 0.5rem 0.9rem;
    width: fit-content;
  }}

  .cover-badge::before {{ content: '★'; }}

  .cover-right {{
    position: relative;
    overflow: hidden;
  }}

  .cover-right img {{
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
    filter: grayscale(8%) contrast(1.05);
    transition: transform 8s ease;
  }}

  .cover-right:hover img {{ transform: scale(1.03); }}

  .cover-right-overlay {{
    position: absolute;
    inset: 0;
    background: linear-gradient(to right, var(--paper) 0%, transparent 20%);
    pointer-events: none;
  }}

  /* ══════════════════════ SECTION CHROME ══════════════════════ */
  section.portfolio-section {{
    padding: 7vw 8vw;
    border-top: var(--rule);
    opacity: 0;
    transform: translateY(28px);
    transition: opacity 0.9s ease, transform 0.9s ease;
  }}

  section.portfolio-section.visible {{
    opacity: 1;
    transform: translateY(0);
  }}

  .s-header {{
    display: grid;
    grid-template-columns: 3rem 1fr;
    gap: 1.5rem;
    align-items: start;
    margin-bottom: 4rem;
  }}

  .s-num {{
    font-family: 'Space Mono', monospace;
    font-size: 0.55rem;
    color: var(--mid);
    letter-spacing: 0.2em;
    padding-top: 0.6rem;
  }}

  .s-title-block {{}}

  .s-label {{
    font-family: 'Space Mono', monospace;
    font-size: 0.55rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--mid);
    display: block;
    margin-bottom: 0.5rem;
  }}

  .s-title {{
    font-size: clamp(2rem, 4vw, 3.8rem);
    font-weight: 300;
    line-height: 1;
    letter-spacing: -0.01em;
  }}

  .s-title em {{ font-style: italic; color: var(--red); }}

  .s-desc {{
    font-size: clamp(0.95rem, 1.6vw, 1.2rem);
    line-height: 1.75;
    color: var(--mid);
    max-width: 65ch;
    margin-top: 1rem;
  }}

  /* ══════════════════════ DARK SECTION ══════════════════════ */
  .dark-section {{
    background: var(--dark-bg);
    color: var(--paper);
    --rule: 1px solid #222018;
    --mid: #6a6250;
  }}

  .dark-section .s-num {{ color: #6a6250; }}
  .dark-section .s-label {{ color: #6a6250; }}
  .dark-section .s-title em {{ color: var(--yellow); }}
  .dark-section .s-desc {{ color: #9a9080; }}

  /* ══════════════════════ PROJECT METADATA ROW ══════════════════════ */
  .meta-row {{
    display: flex;
    flex-wrap: wrap;
    gap: 2.5rem;
    margin-bottom: 3rem;
    padding-bottom: 2rem;
    border-bottom: var(--rule);
  }}

  .meta-item label {{
    font-family: 'Space Mono', monospace;
    font-size: 0.52rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--mid);
    display: block;
    margin-bottom: 0.3rem;
  }}

  .meta-item p {{
    font-size: 1rem;
    line-height: 1.4;
  }}

  /* ══════════════════════ IMAGE UTILITIES ══════════════════════ */
  .img-full {{
    width: 100%;
    display: block;
    cursor: zoom-in;
  }}

  .img-caption {{
    font-family: 'Space Mono', monospace;
    font-size: 0.52rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--mid);
    margin-top: 0.6rem;
  }}

  .img-frame {{
    border: var(--rule);
    overflow: hidden;
    margin-bottom: 1.5rem;
  }}

  /* ══════════════════════ RETICULA — SECTION 01 ══════════════════════ */
  .reticula-layout {{
    display: grid;
    grid-template-columns: 3fr 2fr;
    gap: 3rem;
    align-items: start;
  }}

  .reticula-statement {{
    font-size: clamp(1rem, 1.8vw, 1.35rem);
    font-weight: 300;
    line-height: 1.7;
    color: #9a9080;
    margin-top: 2rem;
  }}

  .award-pill {{
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    border: 1px solid var(--yellow);
    color: var(--yellow);
    font-family: 'Space Mono', monospace;
    font-size: 0.56rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    padding: 0.5rem 1rem;
    margin-top: 2rem;
  }}

  .award-pill::before {{ content: '★'; }}

  /* ══════════════════════ BARCELONA — SECTION 02 ══════════════════════ */
  .barcelona-grid {{
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }}

  .barcelona-aerial {{
    grid-column: 1 / -1;
  }}

  /* ══════════════════════ TOASTER — SECTION 03 ══════════════════════ */
  .toaster-layout {{
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: start;
  }}

  .toaster-arrow {{
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Cormorant Garamond', serif;
    font-size: 2rem;
    color: var(--mid);
    padding: 2rem 0;
  }}

  /* ══════════════════════ STUDIO FINAL — SECTION 04 ══════════════════════ */
  .final-layout {{
    display: grid;
    grid-template-columns: 2fr 3fr;
    gap: 4rem;
    align-items: start;
  }}

  .final-text h3 {{
    font-size: clamp(1.2rem, 2.5vw, 2rem);
    font-weight: 300;
    line-height: 1.2;
    margin-bottom: 1.2rem;
    color: var(--paper);
  }}

  .final-text p {{
    font-size: 1rem;
    line-height: 1.8;
    color: #9a9080;
    margin-bottom: 1rem;
  }}

  /* ══════════════════════ SEMESTER 1 — SECTION 05 ══════════════════════ */
  .sem1-grid {{
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }}

  .sem1-grid .span-2 {{ grid-column: 1 / -1; }}

  /* ══════════════════════ ABOUT — SECTION 06 ══════════════════════ */
  .about-grid {{
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6vw;
  }}

  .about-text h3 {{
    font-size: clamp(1.5rem, 3vw, 2.8rem);
    font-weight: 300;
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }}

  .about-text p {{
    font-size: 1.05rem;
    line-height: 1.8;
    color: var(--mid);
    margin-bottom: 1rem;
  }}

  .stats-col {{
    display: grid;
    gap: 2rem;
    border-left: var(--rule);
    padding-left: 5vw;
    align-content: start;
  }}

  .stat {{ border-bottom: var(--rule); padding-bottom: 1.5rem; }}

  .stat-num {{
    font-size: clamp(2.5rem, 5vw, 4.5rem);
    font-weight: 300;
    color: var(--red);
    line-height: 1;
    font-style: italic;
    margin-bottom: 0.3rem;
  }}

  .stat-label {{
    font-family: 'Space Mono', monospace;
    font-size: 0.58rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--mid);
  }}

  /* ══════════════════════ FOOTER ══════════════════════ */
  footer {{
    background: var(--ink);
    color: var(--paper);
    padding: 5vw 8vw;
    border-top: 3px solid var(--red);
  }}

  .footer-top {{
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    flex-wrap: wrap;
    gap: 2rem;
    padding-bottom: 3rem;
    border-bottom: 1px solid #1e1c14;
  }}

  .footer-name {{
    font-size: clamp(2rem, 5vw, 4rem);
    font-weight: 300;
    line-height: 0.95;
  }}

  .footer-name em {{ font-style: italic; color: var(--yellow); }}

  .footer-info {{
    font-family: 'Space Mono', monospace;
    font-size: 0.6rem;
    letter-spacing: 0.15em;
    line-height: 2.2;
    color: #5a5445;
    text-align: right;
  }}

  .footer-bottom {{
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
    padding-top: 1.5rem;
  }}

  .footer-bottom span {{
    font-family: 'Space Mono', monospace;
    font-size: 0.5rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #2a2820;
  }}

  /* ══════════════════════ LIGHTBOX ══════════════════════ */
  .lightbox {{
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(10,9,5,0.97);
    z-index: 1000;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    cursor: zoom-out;
  }}

  .lightbox.open {{ display: flex; }}

  .lightbox img {{
    max-width: 90vw;
    max-height: 90vh;
    object-fit: contain;
  }}

  .lb-close {{
    position: fixed;
    top: 1.5rem; right: 1.5rem;
    font-family: 'Space Mono', monospace;
    font-size: 0.65rem;
    letter-spacing: 0.18em;
    color: #5a5445;
    cursor: pointer;
    border: 1px solid #2a2820;
    padding: 0.4rem 0.8rem;
    background: transparent;
    transition: color 0.2s, border-color 0.2s;
  }}

  .lb-close:hover {{ color: var(--yellow); border-color: var(--yellow); }}

  /* ══════════════════════ RESPONSIVE ══════════════════════ */
  @media (max-width: 768px) {{
    .cover {{ grid-template-columns: 1fr; }}
    .cover-right {{ height: 50vw; }}
    .cover-right-overlay {{ background: linear-gradient(to bottom, var(--paper) 0%, transparent 30%); }}
    .reticula-layout, .toaster-layout, .final-layout, .about-grid {{ grid-template-columns: 1fr; }}
    .barcelona-grid, .sem1-grid {{ grid-template-columns: 1fr; }}
    .barcelona-aerial {{ grid-column: 1; }}
    .stats-col {{ border-left: none; border-top: var(--rule); padding-left: 0; padding-top: 2rem; }}
    .footer-info {{ text-align: left; }}
    .nav-links {{ display: none; }}
  }}
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <span class="nav-name">Jesus Moya-Gutierrez</span>
  <ul class="nav-links">
    <li><a href="#reticula">Réticula</a></li>
    <li><a href="#barcelona">Barcelona</a></li>
    <li><a href="#toaster">Object Study</a></li>
    <li><a href="#sem1">Year One</a></li>
    <li><a href="#about">About</a></li>
  </ul>
</nav>

<!-- COVER -->
<header class="cover">
  <div class="cover-left">
    <span class="cover-eyebrow">Architecture Portfolio — 2024–2026</span>
    <h1 class="cover-name">Jesus<br><em>Moya-</em><br>Gutierrez</h1>
    <div class="cover-details">
      <span>B.Arch — University of Illinois at Chicago</span>
      <span>School of Architecture · Year One</span>
      <span>Chicago, Illinois</span>
    </div>
    <div class="cover-badge">Selected — Super Jury Presentation · Spring 2025</div>
  </div>
  <div class="cover-right">
    <img src="data:image/jpeg;base64,{imgs['reticula']}" alt="Réticula Axonometric">
    <div class="cover-right-overlay"></div>
  </div>
</header>

<!-- ══ 01 RÉTICULA ══ -->
<section class="portfolio-section dark-section" id="reticula">
  <div class="s-header">
    <span class="s-num">01</span>
    <div class="s-title-block">
      <span class="s-label">Spring 2025 · Studio Final</span>
      <h2 class="s-title">Réticula <em>Sandwich</em></h2>
      <p class="s-desc">
        Réticula collides small residential houses with monumental chamfered superblocks. 
        Larger blocks form a dense grid of offices, civic, educational, governmental, and 
        commercial spaces, while sparse residential neighborhoods are layered into the ground 
        and gaps between them. Residential pathways cross above, below, and through the grid.
      </p>
    </div>
  </div>

  <div class="meta-row">
    <div class="meta-item"><label>Course</label><p>ARCH 106 — Architectural Urbanism</p></div>
    <div class="meta-item"><label>Section</label><p>Turner</p></div>
    <div class="meta-item"><label>Scale</label><p>1:500 / 1"=60'</p></div>
    <div class="meta-item"><label>Medium</label><p>Digital — Ink on Paper</p></div>
  </div>

  <div class="reticula-layout">
    <div>
      <div class="img-frame">
        <img class="img-full" src="data:image/jpeg;base64,{imgs['reticula']}" alt="Réticula Collective Form Axonometric">
      </div>
      <p class="img-caption">Collective Form Axonometric — 660'×660' — Scale 1:500</p>
    </div>
    <div>
      <div class="img-frame">
        <img class="img-full" src="data:image/jpeg;base64,{imgs['a6_final']}" alt="Réticula Full Sheet — Plan, Section, Axonometric">
      </div>
      <p class="img-caption">Full Submission Sheet — Plan · Section · Axonometric</p>
      <div class="award-pill">Selected for Super Jury — Spring 2025</div>
    </div>
  </div>
</section>

<!-- ══ 02 BARCELONA ══ -->
<section class="portfolio-section" id="barcelona">
  <div class="s-header">
    <span class="s-num">02</span>
    <div class="s-title-block">
      <span class="s-label">Spring 2026 · Urban Analysis</span>
      <h2 class="s-title">Half-Mile Urban <em>Plan</em></h2>
      <p class="s-desc">
        A systematic urban analysis of a half-mile × half-mile tile of Barcelona. 
        Four drawings — aerial, figure-ground, network, and program — dissect the Eixample 
        grid's organization, movement systems, and programmatic density.
      </p>
    </div>
  </div>

  <div class="meta-row">
    <div class="meta-item"><label>Course</label><p>ARCH 106 — Spring 2026</p></div>
    <div class="meta-item"><label>City</label><p>Barcelona, Spain</p></div>
    <div class="meta-item"><label>Tile</label><p>#5</p></div>
    <div class="meta-item"><label>Collaborators</label><p>Jesus Moya · Namuna Naranbaatar</p></div>
  </div>

  <div class="barcelona-grid">
    <div class="barcelona-aerial">
      <div class="img-frame">
        <img class="img-full" src="data:image/jpeg;base64,{imgs['aerial']}" alt="Barcelona Aerial">
      </div>
      <p class="img-caption">A1 — Aerial View · Half Mile Urban Plan</p>
    </div>
    <div>
      <div class="img-frame">
        <img class="img-full" src="data:image/jpeg;base64,{imgs['figureground']}" alt="Figure Ground">
      </div>
      <p class="img-caption">Figure Ground</p>
    </div>
    <div>
      <div class="img-frame">
        <img class="img-full" src="data:image/jpeg;base64,{imgs['program']}" alt="Program">
      </div>
      <p class="img-caption">Program</p>
    </div>
    <div style="grid-column: 1/-1">
      <div class="img-frame">
        <img class="img-full" src="data:image/jpeg;base64,{imgs['network']}" alt="Network">
      </div>
      <p class="img-caption">Network — Streets · Routes · Stations</p>
    </div>
  </div>
</section>

<!-- ══ 03 TOASTER / OBJECT STUDY ══ -->
<section class="portfolio-section dark-section" id="toaster">
  <div class="s-header">
    <span class="s-num">03</span>
    <div class="s-title-block">
      <span class="s-label">Year One · Object Study</span>
      <h2 class="s-title">Object <em>Transformation</em></h2>
      <p class="s-desc">
        Starting from a Dualit toaster, the study explores how a familiar domestic object 
        can be morphed through repetition, extension, and formal logic — 
        the result is a dramatically elongated version that retains the toaster's essential 
        identity while transforming its scale and spatial presence.
      </p>
    </div>
  </div>

  <div class="meta-row">
    <div class="meta-item"><label>Object</label><p>Dualit Classic Toaster</p></div>
    <div class="meta-item"><label>Method</label><p>Morphological Transformation</p></div>
    <div class="meta-item"><label>Medium</label><p>Digital Rendering</p></div>
  </div>

  <div class="toaster-layout">
    <div>
      <div class="img-frame">
        <img class="img-full" src="data:image/jpeg;base64,{imgs['toaster1']}" alt="Dualit Toaster — Original">
      </div>
      <p class="img-caption">Before — Dualit Classic 2-Slot Toaster</p>
    </div>
    <div>
      <div class="img-frame">
        <img class="img-full" src="data:image/jpeg;base64,{imgs['toaster2']}" alt="Morphed Toaster — Extended">
      </div>
      <p class="img-caption">After — Extended Multi-Slot Transformation</p>
    </div>
  </div>
</section>

<!-- ══ 04 SEMESTER 1 DRAWINGS ══ -->
<section class="portfolio-section" id="sem1">
  <div class="s-header">
    <span class="s-num">04</span>
    <div class="s-title-block">
      <span class="s-label">Fall 2024 · ARCH 105 — Studio I</span>
      <h2 class="s-title">First Semester <em>Drawings</em></h2>
      <p class="s-desc">
        Hand drawings exploring two-point perspective, form, texture, materiality, and structural 
        massing. Each work is built through direct observation and constructed line — 
        drawing as both method and artifact.
      </p>
    </div>
  </div>

  <div class="sem1-grid">
    <div class="span-2">
      <div class="img-frame">
        <img class="img-full" src="data:image/jpeg;base64,{imgs['colored_perspective']}" alt="Colored pencil perspective drawing">
      </div>
      <p class="img-caption">Two-Point Perspective · Colored Pencil on Paper · Brick Building Complex</p>
    </div>
    <div>
      <div class="img-frame">
        <img class="img-full" src="data:image/jpeg;base64,{imgs['floor_plan']}" alt="Plan drawings">
      </div>
      <p class="img-caption">Collective Form — Plan Drawings</p>
    </div>
    <div>
      <div class="img-frame">
        <img class="img-full" src="data:image/jpeg;base64,{imgs['wood_model']}" alt="Wood block model">
      </div>
      <p class="img-caption">Physical Model · Wood Blocks — Massing Study</p>
    </div>
  </div>
</section>

<!-- ══ 05 ABOUT ══ -->
<section class="portfolio-section" id="about">
  <div class="s-header">
    <span class="s-num">05</span>
    <div class="s-title-block">
      <span class="s-label">Profile</span>
      <h2 class="s-title">Jesus <em>Moya-Gutierrez</em></h2>
    </div>
  </div>

  <div class="about-grid">
    <div class="about-text">
      <h3>Drawing as<br>primary language.</h3>
      <p>
        I am a first-year architecture student at the University of Illinois at Chicago, 
        approaching design through the discipline of the hand-drawn line and spatial analysis.
        Drawing, for me, is not documentation — it is thinking made visible.
      </p>
      <p>
        My Spring 2025 final project, <em>Réticula Sandwich</em>, was selected from a cohort of 
        twenty students to be presented before an external super jury — architecture 
        professors from institutions across the country.
      </p>
      <p>
        My work spans urban analysis, object transformation, perspectival drawing, 
        physical model-making, and digital visualization. I am drawn to collective form, 
        urban density, and the friction between neighborhood scale and monumental infrastructure.
      </p>
    </div>
    <div class="stats-col">
      <div class="stat">
        <div class="stat-num">1st</div>
        <div class="stat-label">Year, B.Arch — UIC School of Architecture</div>
      </div>
      <div class="stat">
        <div class="stat-num">1/20</div>
        <div class="stat-label">Selected for Super Jury — Spring 2025</div>
      </div>
      <div class="stat">
        <div class="stat-num">4</div>
        <div class="stat-label">Projects Across Two Semesters</div>
      </div>
      <div class="stat">
        <div class="stat-num">UIC</div>
        <div class="stat-label">Chicago, Illinois</div>
      </div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-top">
    <div class="footer-name">
      Jesus<br><em>Moya-Gutierrez</em>
    </div>
    <div class="footer-info">
      Architecture Student<br>
      University of Illinois at Chicago<br>
      School of Architecture<br>
      Chicago, Illinois · 2024–2026
    </div>
  </div>
  <div class="footer-bottom">
    <span>Jesus Moya-Gutierrez · Architecture Portfolio 2024–2026</span>
    <span>UIC School of Architecture</span>
  </div>
</footer>

<!-- LIGHTBOX -->
<div class="lightbox" id="lightbox">
  <button class="lb-close" id="lbClose">Close ×</button>
  <img id="lbImg" src="" alt="">
</div>

<script>
  // Scroll reveal
  const obs = new IntersectionObserver(entries => {{
    entries.forEach(e => {{ if (e.isIntersecting) e.target.classList.add('visible'); }});
  }}, {{ threshold: 0.08 }});
  document.querySelectorAll('.portfolio-section').forEach(s => obs.observe(s));

  // Lightbox
  const lb = document.getElementById('lightbox');
  const lbImg = document.getElementById('lbImg');

  document.querySelectorAll('.img-full').forEach(img => {{
    img.addEventListener('click', () => {{
      lbImg.src = img.src;
      lb.classList.add('open');
    }});
  }});

  const closeLb = () => lb.classList.remove('open');
  document.getElementById('lbClose').addEventListener('click', closeLb);
  lb.addEventListener('click', e => {{ if (e.target === lb) closeLb(); }});
  document.addEventListener('keydown', e => {{ if (e.key === 'Escape') closeLb(); }});
</script>
</body>
</html>'''

with open('/mnt/user-data/outputs/jesus_moya_gutierrez_portfolio.html', 'w') as f:
    f.write(html)

print(f'Done — {len(html):,} chars')
PYEOF